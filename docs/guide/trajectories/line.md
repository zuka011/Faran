# Line Trajectories

Straight path between two endpoints.

## Usage

```python
from faran.numpy import trajectory

reference = trajectory.line(start=(0.0, 0.0), end=(10.0, 0.0), path_length=10.0)
```

## Parameters

| Parameter | Description |
|-----------|-------------|
| `start` | Start point as `(x, y)` tuple. |
| `end` | End point as `(x, y)` tuple. |
| `path_length` | Parameterization length. |

## API Reference

See the [trajectory API reference](../../api/trajectory.md) for full signatures.
